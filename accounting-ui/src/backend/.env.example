MYSQLHOST=
MYSQLPORT=
MYSQLDATABASE=
MYSQLUSER=
MYSQLPASSWORD=
JWT_SECRET=
# Server-only secret for invoice verification HMAC signing (see
# invoiceVerificationService.js) - never sent to the frontend.
HMAC_SECRET=
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-5
# mock (default, no API key needed) | anthropic | local (currently a mock alias)
AI_PROVIDER=mock